(function () { 
  if (!mstrmojo.plugins.{{name}}) {
    mstrmojo.plugins.{{name}} = {};
  }

  mstrmojo.requiresCls(
    'mstrmojo.vi.models.CustomVisDropZones',
    'mstrmojo.array'
  )

  mstrmojo.plugins.{{name}}.{{name}}DropZones = mstrmojo.declare(
    mstrmojo.vi.models.CustomVisDropZones,
    null,
    {
      scriptClass: 'mstrmojo.plugins.{{ name }}.{{ name }}DropZones',
      cssClass: '{{lower name}}dropzones',
      getCustomDropZones: function getCustomDropZones(){
    },
      shouldAllowObjectsInDropZone: function (zone, dragObjects, idx, edge, context) {
      },
      getActionsForObjectsDropped: function (zone, droppedObjects, idx, replaceObject, extras) {
      },
      getActionsForObjectsRemoved: function (zone, objects) { 
      },
      getDropZoneContextMenuItems: function (cfg, zone, object, el) {
      }
    }
  )
}())
